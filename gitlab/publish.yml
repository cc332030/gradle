
include:
  - https://cnb.cool/cc332030/gradle/-/blob/master/gitlab/publish-job.yml

.publish: &publish
  extends: .publish-job

"(main|master)":
  pull_request:
    - <<: *publish
  push:
    - <<: *publish
$:
  tag_push:
    - <<: *publish
